<div class="container mt-5">
  <h1><%= heading %></h1>

  <%= form_with model: @course, local: true, class: "form" do |f| %>
    <div class="mb-3">
      <%= f.label :name, class: "form-label" %>
      <%= f.text_field :name, class: "form-control" %>
    </div>
    <div class="mb-3">
      <%= f.label :students, class: "form-label" %>
      <%= f.collection_select :student_ids, Student.all, :id, :name, { selected: @course.student_ids }, { multiple: true, class: "form-select" } %>
    </div>
    <%= f.submit button_text, class: "btn btn-success" %>
  <% end %>
</div>
